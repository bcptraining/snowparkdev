{
  "name": "Snowpark Dev",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "features": {
    "conda": "latest"
  },
  "postCreateCommand": "conda config --set channel_priority flexible && conda env create -f environment.yml && pip install pipx && pipx ensurepath && pipx install snowflake-cli && echo 'export PATH=\"$HOME/.local/bin:$PATH\"' >> ~/.bashrc && export PATH=\"$HOME/.local/bin:$PATH\" && chmod +x .devcontainer/setup.sh && .devcontainer/setup.sh",

  "customizations": {
    "vscode": {
      "settings": {
        "python.pythonPath": "/usr/local/bin/python"
      },
      "extensions": ["ms-python.python", "ms-toolsai.jupyter"]
    }
  },
  "remoteUser": "vscode"
}
