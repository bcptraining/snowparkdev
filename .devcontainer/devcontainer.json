{
  "name": "Snowpark Dev",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "features": {
    "conda": "latest"
  },
  "postCreateCommand": "conda config --set channel_priority flexible && conda env create -f environment.yml && curl -sSL https://install.snowflake.com/snowcli/install.sh | bash && echo 'export PATH=\"$HOME/.snowflake/bin:$PATH\"' >> ~/.bashrc && export PATH=\"$HOME/.snowflake/bin:$PATH\" && chmod +x .devcontainer/setup.sh && .devcontainer/setup.sh",

  "customizations": {
    "vscode": {
      "settings": {
        "python.pythonPath": "/usr/local/bin/python"
      },
      "extensions": ["ms-python.python", "ms-toolsai.jupyter"]
    }
  },
  "remoteUser": "vscode"
}
