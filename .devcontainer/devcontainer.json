{
  "name": "Snowpark Dev",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "features": {
    "ghcr.io/devcontainers/features/conda:1": {}
  },
  "postCreateCommand": "conda config --set channel_priority flexible && conda env create -f environment.yml && echo '✅ Environment ready!' && curl -sSL https://install.snowflake.com/snowcli/install.sh | bash && echo 'export PATH=\"$HOME/.snowflake/bin:$PATH\"' >> ~/.bashrc && export PATH=\"$HOME/.snowflake/bin:$PATH\" && snow --version",
  "remoteUser": "root",
  "overrideCommand": true,
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/opt/conda/envs/py311_env/bin/python"
      },
      "extensions": ["ms-python.python"]
    }
  }
}
# This file is used to define the development container configuration for Snowpark development.